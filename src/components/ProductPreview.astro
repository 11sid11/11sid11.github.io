---
import type { DevProject } from '@/data/projects';

interface Props {
  project: DevProject;
  compact?: boolean;
}

const { project, compact = false } = Astro.props;
const { slug, title, preview } = project;
---
<div class:list={['product-preview', `preview-${slug}`, compact && 'is-compact']} aria-hidden="true">
  <div class="preview-browserbar">
    <span></span><span></span><span></span>
    <div class="preview-url">{preview.address}</div>
  </div>

  <div class="preview-stage">
    {preview.src ? (
      <img
        class="product-preview-image"
        src={preview.src}
        alt=""
        loading="lazy"
        decoding="async"
        width="1280"
        height="800"
      />
    ) : (
      <div class="private-preview">
        <span class="private-preview-status">PRIVATE LAB / COMING SOON</span>
        <strong>{title}</strong>
        <p>Diet, training and everyday decision support—being tested before it is explained.</p>
        <div class="private-preview-track"><i></i></div>
        <span class="private-preview-note">HIGH-LEVEL VIEW ONLY</span>
      </div>
    )}
  </div>

  <div class="preview-caption">
    <span>{title}</span>
    <span>{preview.src ? 'AUTHENTIC CAPTURE' : 'PRIVATE PREVIEW'}</span>
  </div>
</div>
